name: assign_variable
description: A Dart lesson on assigning values to variables using the assignment operator (=)
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
