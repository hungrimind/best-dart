class Book {
  String title;
  String author;
  int publicationYear;

  // Create a custom constructor that accepts three parameters:
  // a String for title, a String for author, and an int for publicationYear.
  // Use these parameters to initialize the properties above.
}

void main() {
  // Create an instance of Book using your custom constructor.
  // Set the title to "The Dart Guide", author to "John Doe", and publicationYear to 2023.
  
  // Print each property (title, author, publicationYear) to verify they were initialized correctly.
}

