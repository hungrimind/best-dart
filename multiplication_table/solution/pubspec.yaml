name: multiplication_table
description: A program that generates a multiplication table for a user-provided number using a for loop.
version: 1.0.0

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0

