// main.dart

void main() {
  // You know you will need a 'summary' string later, but you can't build it yet.
  // TODO: Declare a non-nullable String variable named 'summary'.

  // Other variables are defined and initialized first.
  String userName = "Tadas";
  int userAge = 28;

  // Now that you have the required information, you can build the summary string.
  summary = "$userName is $userAge years old.";

  // Finally, print the summary. This line must work.
  print(summary);
}
