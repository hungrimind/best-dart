name: grade_calculator_switch
description: Learn to use switch statements with relational patterns to categorize numerical scores
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0

